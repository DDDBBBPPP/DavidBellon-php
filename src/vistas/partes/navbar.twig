<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid">

        <a class="navbar-brand" href="#">
            Gestor Hotelero
        </a>

        <ul class="navbar-nav me-auto mb-2 mb-lg-0">

            {#  CLIENTE  #}
            {% if es_cliente() %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('cliente.inicio') }}">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('cliente.habitaciones') }}">Habitaciones</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('cliente.reservas') }}">Mis reservas</a>
                </li>
            {% endif %}

            {#  ADMIN  #}
            {% if es_admin() %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('admin.inicio') }}">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('admin.reservas') }}">Reservas</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('admin.habitaciones') }}">Habitaciones</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ ruta('admin.clientes') }}">Clientes</a>
                </li>
            {% endif %}
        </ul>


        <div class="d-flex align-items-center gap-3">
            <span class="text-white">
                {{ usuario().getNombre() }}
            </span>
            <a class="nav-link text-white-50 p-0" href="{{ ruta('logout') }}">Salir</a>
        </div>

    </div>
</nav>
